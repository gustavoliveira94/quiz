import styled from 'styled-components';

const ContentAnswer = styled.div`
    display: flex;
    align-items: center;
    padding: 10px 0;

    button {
        width: 100%;
    }

    p {
        margin-left: 10px;
    }
`;

export default ContentAnswer;

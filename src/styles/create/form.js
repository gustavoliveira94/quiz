import styled from 'styled-components';

const Form = styled.form`
    position: relative;
    width: 600px;
    height: 800px;
    background-color: #fff;
    padding: 15px;
    overflow: scroll;

    label {
        color: #000;
    }
`;

export default Form;
